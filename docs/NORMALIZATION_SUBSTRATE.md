# Normalization Substrate

## 1. Purpose

The normalization substrate provides a stable mathematical foundation for all Vantage 2.0 inputs. Raw signals are transformed into the closed interval [0,1] to ensure manifold stability, suppress derivative jitter, and maintain consistent behavior across heterogeneous data sources. This substrate defines the rules, invariants, and transformations that convert real-world signals into physics-native state variables.
